using Platformer.Mechanics;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PointsManager : MonoBehaviour
{
    //public static PointsManager Instance = null;

    //private int FeedCount;
    //private int ObediencePointsCount;

    //private void Start()
    //{
    //    if (Instance == null) Instance = this;
    //    FeedCount = PlayerController.Instance.CountOfFeed;
    //    ObediencePointsCount = PlayerController.Instance.CountOfObediencePoints;
    //}

    //public void SaveCurrentPoints()
    //{
    //    PlayerPrefs.SetInt("FeedCount", FeedCount);
    //    PlayerPrefs.SetInt("ObediencePoints", ObediencePointsCount);
    //}
}
